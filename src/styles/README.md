/**

- @dir styles
- Global shared styles, CSS modules, and animation utilities.
-
- Files to add here:
- - animations.css — reusable keyframe animations
- - variables.css — extra CSS custom properties beyond globals.css
- - typography.css — heading/body typographic scale
-
- Note: Tailwind utility classes should be preferred over custom CSS.
- Only add files here when Tailwind cannot express the style.
  */
