/**
 * @module services
 * API service layer — abstraction for all external data-fetching.
 *
 * Services encapsulate the "how" of data access so that hooks and components
 * only deal with the "what". This makes it easy to swap Supabase for Firebase
 * or any other backend without changing component code.
 *
 * Planned services:
 * - analyticsService  (Phase 13/16)
 * - storageService    (file uploads — Phase 5)
 * - exportService     (cloud export pipeline — Phase 8)
 * - aiService         (AI text/layout generation — Phase 10)
 * - billingService    (Stripe subscriptions — Phase 12)
 */

// Placeholder — individual services will be added per phase
export {}
